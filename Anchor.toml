[toolchain]
anchor_version = "1.2.0"

[features]
resolution = true
skip-lint = false

[programs.localnet]
# oracle = "<program id — generated once `anchor keys sync` runs on a real program>"
# demo_pool = "<program id — same>"

[registry]
url = "https://api.apr.dev"

[provider]
cluster = "localnet"
wallet = "~/.config/solana/tee-income-oracle.json"

[scripts]
test = "echo \"no tests yet\""
